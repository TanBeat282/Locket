<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Locket" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Locket" parent="Base.Theme.Locket">
        <item name="android:statusBarColor">@color/bg</item> <!-- Màu của thanh trạng thái -->
        <item name="android:windowLightStatusBar">false
        </item> <!-- Nội dung thanh trạng thái màu sáng -->
        <item name="android:navigationBarColor">@color/bg</item> <!-- Màu của thanh điều hướng -->
        <item name="android:windowLightNavigationBar" tools:ignore="NewApi">false
        </item> <!-- Nội dung thanh điều hướng màu sáng -->
    </style>
    <!-- res/values/styles.xml -->
    <style name="HintTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textSize">@dimen/_12ssp</item>
        <item name="android:textColorHint">@color/hint</item>
        <item name="android:fontFamily">@font/black</item>
    </style>

    <!-- res/values/styles.xml -->
    <style name="HintTextAppearance2" parent="TextAppearance.AppCompat">
        <item name="android:textSize">@dimen/_12ssp</item>
        <item name="android:fontFamily">@font/black</item>
    </style>

    <style name="ShapeAppearance.App.CornerSize50Percent" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50dp</item>
    </style>

    <style name="RoundedCorners">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item> <!-- Điều chỉnh kích thước góc bo -->
    </style>


<!--    Bottomsheet-->
    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <!-- Thiết lập background của BottomSheet thành trong suốt -->
        <item name="bottomSheetStyle">@style/CustomBottomSheet</item>
    </style>

    <style name="CustomBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
        <!-- Thiết lập góc bo tròn cho BottomSheet -->
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.BottomSheet</item>
    </style>

    <style name="ShapeAppearanceOverlay.BottomSheet" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">@dimen/_30sdp</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerFamilyRounded"  parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">@dimen/_50sdp</item> <!-- Điều chỉnh bán kính bo góc -->
    </style>

</resources>